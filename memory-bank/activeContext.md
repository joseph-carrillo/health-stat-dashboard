# activeContext.md

## Current Session Goal
Writing Python seed script to auto-generate all indicators.

## What Was Just Completed
- Project folder skeleton created and committed
- .gitignore and docker-compose.yml created
- PostgreSQL running in Docker — verified
- DBeaver connected — verified
- All 7 database tables created (schema.sql)
- 128 NIR locations seeded (seed_locations.sql)
- 11 programs seeded (seed_programs.sql)
- 34 report periods seeded (seed_periods.sql)
- 21 Immunization File 1 indicators seeded manually (learning exercise)
- Decided: remaining indicators will be auto-generated by Python script

## What Happens Next (Start Here)
Write the Python seed script:
- File: backend/app/core/seed_indicators.py
- Reads a structured config (JSON or Python dict)
- Generates and runs INSERT statements for all indicators
- Handles all programs and subfolders in one run

## After Seed Script
1. Write first parser config JSON (Immunization File 1)
2. Write Excel parser service (Python)
3. Write validation engine
4. Build FastAPI endpoints
5. Build auth and RBAC
6. Build frontend (last)

## Local Database (Development)
- Host: localhost | Port: 5432
- Database: doh_nir_dashboard
- Username: doh_admin | Password: doh_password_2026
- Start with: docker-compose up -d
- Stop with: docker-compose down

## Monday Office Desktop Checklist
1. git pull origin main
2. Start Docker Desktop
3. docker-compose up -d
4. Open DBeaver — connection already saved

## Critical Reference
fhsis_template_analysis.md is in project knowledge.
Always search it before making any parser or schema decisions.