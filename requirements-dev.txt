# Development/test dependencies (not installed in the production image).
# Install in the running backend container to run tests:
#   docker compose exec backend pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
ruff==0.15.20
# Required by starlette.testclient (FastAPI TestClient) for endpoint tests.
httpx2==2.5.0
