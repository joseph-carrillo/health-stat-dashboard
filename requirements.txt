# Exact versions — these are what's actually installed and running (pinned
# from `pip freeze` in the backend container, image built 2026-06-18).
# Bump deliberately: edit a version here, rebuild, re-run the test suite.
fastapi==0.137.1
uvicorn[standard]==0.49.0
psycopg2-binary==2.9.12
pandas==3.0.3
openpyxl==3.1.5
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# Pin bcrypt to the 4.0.x line: passlib 1.7.4's backend detection breaks on
# bcrypt >=4.1 ("password cannot be longer than 72 bytes"). bcrypt is kept
# (verify-only) so legacy hashes still log in; new hashes are argon2 and each
# legacy hash upgrades on first successful login.
bcrypt==4.0.1
argon2-cffi==25.1.0
python-multipart==0.0.32
gunicorn==26.0.0
# Login rate limiting (in-memory, per worker).
slowapi==0.1.9
