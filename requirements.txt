fastapi>=0.111.0
uvicorn[standard]>=0.29.0
psycopg2-binary>=2.9.9
pandas>=2.2.0
openpyxl>=3.1.2
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pin bcrypt to the 4.0.x line: passlib 1.7.4's backend detection breaks on
# bcrypt >=4.1 ("password cannot be longer than 72 bytes"). Revisit if/when we
# migrate hashing to argon2.
bcrypt>=4.0,<4.1
python-multipart>=0.0.9
gunicorn>=21.2.0
